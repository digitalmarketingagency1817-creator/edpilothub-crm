import { PlaybookClient } from "@/components/crm/playbook-client";

export const metadata = {
  title: "Sales Playbook — EdPilotHub CRM",
};

export default function PlaybookPage() {
  return <PlaybookClient />;
}
