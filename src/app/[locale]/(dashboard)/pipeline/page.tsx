import { PipelineClient } from "@/components/pipeline/pipeline-client";

export const metadata = {
  title: "Pipeline — EdPilotHub CRM",
};

export default function PipelinePage() {
  return <PipelineClient />;
}
